{
  "name": "proxy-server",
  "version": "1.0.0",
  "description": "Proxy server for API requests",
  "main": "server.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.0.0"
  },
  "engines": {
    "node": ">=18.x"
  }
}
